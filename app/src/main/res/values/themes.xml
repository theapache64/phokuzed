<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Phokuzed" parent="android:Theme.DeviceDefault.NoActionBar">
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/daintree</item>
        <item name="android:background">@color/daintree</item>
        <item name="android:windowBackground">@color/daintree</item>
        <!-- Customize your theme here. -->
    </style>

</resources>